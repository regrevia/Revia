# Hello Track

Input: [`examples/hello.re`](../../../examples/hello.re).

Acceptance after publication: the sealed trial manifest must bind the source SHA-256, exact CLI command, exit status, and canonical result digest. This track does not use the old preview runtime as a fallback.

中文：发布后以密封 trial manifest 绑定源码摘要、精确命令、退出状态与规范结果摘要；不得回退到旧预览运行时。
