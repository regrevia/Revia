# Project workflow fixture

The logical trial creates a fresh project under `actual`, checks it, and runs its native project tests. The `expected` directory records the byte-exact generated project used during development-side measurement.
