# Agent Review Track

Input: [`examples/agent-review/main.re`](../../../examples/agent-review/main.re).

Acceptance after publication: the candidate must expose the same declared capability and both closed result branches through its checked machine evidence. Any human-facing projection must be traceable to that same evidence.

中文：候选版本必须在已检查机器证据中暴露同一能力声明与两个闭合结果分支；人类视图必须可追溯到同一证据。
