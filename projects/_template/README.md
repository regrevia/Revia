# Project Name / 项目名称

## Goal / 目标

State the problem this project explores. / 说明该项目探索的问题。

## Run / 运行

```bash
./bin/revia check projects/<directory>/main.re
./bin/revia run projects/<directory>/main.re
./bin/revia manifest projects/<directory>/main.re
```

## Result / 结果

Record output, generated artifacts, and one conclusion.
记录输出、生成产物与一条结论。
