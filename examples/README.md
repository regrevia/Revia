# Examples

- `hello.re`: successful check and run path.
- `agent-business-starter.re`: small file for an Agent to edit and re-check.
- `diagnostic-error.re`: intentionally invalid argument name for testing
  structured diagnostics. A `check` exit status of `65` is expected.

Run every command from the repository root so paths in output are easy to
compare across reports.
